name: iOS CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Xcode (إصلاح: استخدام إصدار متاح)
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.4'

      - name: Show Xcode version for verification
        run: |
          echo "📱 إصدار Xcode المثبت:"
          xcodebuild -version
          echo ""
          echo "🔧 إصدار Swift:"
          swift --version

      # ... باقي الخطوات تأتي هنا بنفس نمط المسافات
